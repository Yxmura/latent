diff --git a/src/llama-arch.cpp b/src/llama-arch.cpp
index 0899696..27841f8 100644
--- a/src/llama-arch.cpp
+++ b/src/llama-arch.cpp
@@ -392,6 +392,7 @@ static const std::map<llm_tensor, const char *> LLM_TENSOR_NAMES = {
     { LLM_TENSOR_FFN_EXP_PROBS_B,                        "blk.%d.exp_probs_b" },
     { LLM_TENSOR_FFN_LATENT_DOWN,                        "blk.%d.ffn_latent_down" },
     { LLM_TENSOR_FFN_LATENT_UP,                          "blk.%d.ffn_latent_up" },
+    { LLM_TENSOR_FFN_LATENT_GATE,                        "blk.%d.ffn_latent_gate" },
     { LLM_TENSOR_ATTN_NORM_2,                            "blk.%d.attn_norm_2" },
     { LLM_TENSOR_ATTN_QKV,                               "blk.%d.attn_qkv" },
     { LLM_TENSOR_LAYER_OUT_NORM,                         "blk.%d.layer_output_norm" },
diff --git a/src/llama-arch.h b/src/llama-arch.h
index d346f14..41ede4e 100644
--- a/src/llama-arch.h
+++ b/src/llama-arch.h
@@ -412,6 +412,7 @@ enum llm_tensor {
     LLM_TENSOR_FFN_EXP_PROBS_B,
     LLM_TENSOR_FFN_LATENT_DOWN,
     LLM_TENSOR_FFN_LATENT_UP,
+    LLM_TENSOR_FFN_LATENT_GATE,
     LLM_TENSOR_ATTN_Q_NORM,
     LLM_TENSOR_ATTN_K_NORM,
     LLM_TENSOR_LAYER_OUT_NORM,
diff --git a/src/llama-model.h b/src/llama-model.h
index 60561a2..f6ce177 100644
--- a/src/llama-model.h
+++ b/src/llama-model.h
@@ -314,6 +314,7 @@ struct llama_layer {
     // ff MoE latent proj
     struct ggml_tensor * ffn_latent_down = nullptr;
     struct ggml_tensor * ffn_latent_up   = nullptr;
+    struct ggml_tensor * ffn_latent_gate = nullptr;
 
     // ff shared expert (shexp)
     struct ggml_tensor * ffn_gate_inp_shexp = nullptr;
diff --git a/src/models/deepseek4.cpp b/src/models/deepseek4.cpp
index c45f9a0..5465205 100644
--- a/src/models/deepseek4.cpp
+++ b/src/models/deepseek4.cpp
@@ -152,6 +152,11 @@ void llama_model_deepseek4::load_arch_tensors(llama_model_loader &) {
         layer.ffn_down_exps = create_tensor(tn(LLM_TENSOR_FFN_DOWN_EXPS, "weight", i), {n_ff_exp, n_embd,   n_expert}, 0);
         layer.ffn_up_exps   = create_tensor(tn(LLM_TENSOR_FFN_UP_EXPS,   "weight", i), {n_embd,   n_ff_exp, n_expert}, 0);
 
+        // LATENT: optional factor projections, only present in LATENT-quantized GGUFs
+        layer.ffn_latent_gate = create_tensor(tn(LLM_TENSOR_FFN_LATENT_GATE, "weight", i), {n_embd,   n_ff_exp}, TENSOR_NOT_REQUIRED);
+        layer.ffn_latent_up   = create_tensor(tn(LLM_TENSOR_FFN_LATENT_UP,   "weight", i), {n_embd,   n_ff_exp}, TENSOR_NOT_REQUIRED);
+        layer.ffn_latent_down = create_tensor(tn(LLM_TENSOR_FFN_LATENT_DOWN, "weight", i), {n_ff_exp, n_embd},   TENSOR_NOT_REQUIRED);
+
         layer.ffn_gate_shexp = create_tensor(tn(LLM_TENSOR_FFN_GATE_SHEXP, "weight", i), {n_embd,   n_ff_exp * n_expert_shared}, 0);
         layer.ffn_down_shexp = create_tensor(tn(LLM_TENSOR_FFN_DOWN_SHEXP, "weight", i), {n_ff_exp * n_expert_shared, n_embd}, 0);
         layer.ffn_up_shexp   = create_tensor(tn(LLM_TENSOR_FFN_UP_SHEXP,   "weight", i), {n_embd,   n_ff_exp * n_expert_shared}, 0);
